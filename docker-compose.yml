version: '3'
services:
  tg_proxy:
    image: nineseconds/mtg:2
    container_name: tg_proxy
    restart: unless-stopped
    ports:
      - "8443:3128"
    command: run -s "${TG_SECRET}" -a 0.0.0.0:3128
