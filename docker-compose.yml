version: '3'
services:
  tg_proxy:
    image: nineseconds/mtg:2
    container_name: tg_proxy
    restart: unless-stopped
    ports:
      - "8443:3128"
    command: simple-run -n 1.1.1.1 -t google.com 0.0.0.0:3128 ${TG_SECRET}


